export enum Status {
  OPEN = 'Open',
  IN_PROGRESS = 'In Progress',
  CLOSED = 'Closed',
  CANCELLED = 'Cancelled',
}

export enum Response {
  APPROVE = 'Approve',
  REJECT = 'Reject',
  PENDING = 'Pending',
}

export enum Priority {
  NORMAL = 'Normal',
  HIGH = 'High',
  LOW = 'Low',
}
